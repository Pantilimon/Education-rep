def funck(ar1, ar2):
    tot = () - //словарь мапа
    keys = []
    for i in (arr1+arr2):
    if i.id not in keys:
        keys.append(i.id)
        tot.append(i.id ->i)
    else:
        tot[i.id] = f(tot[i.id], i)
    tot -> to list
    res = []
    tot.sort(fun (a, b) {
        return a[0]-a[0]
    })
    for i in tot:
        res.append(i[1])